# SoftTurn Kiosk Frontend

Separate project for customer self-service kiosk.

## Run

1. Copy `.env.example` to `.env.local`.
2. Set `NEXT_PUBLIC_KIOSK_BRANCH_ID`.
3. Install dependencies: `npm install`.
4. Start: `npm run dev`.

This app only exposes kiosk flow on route `/`.
