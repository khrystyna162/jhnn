# SoftTurn Display Frontend

Separate project for branch hall display screen.

## Run

1. Copy `.env.example` to `.env.local`.
2. Set `NEXT_PUBLIC_DISPLAY_BRANCH_ID`.
3. Install dependencies: `npm install`.
4. Start: `npm run dev`.

This app only exposes display flow on route `/`.
